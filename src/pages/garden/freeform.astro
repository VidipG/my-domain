---
import { getCollection } from 'astro:content';
import PageLayout from '@/layouts/PageLayout.astro';
import NoteCard from '@/components/garden/NoteCard.astro';

const notes = (await getCollection('notes', (n) => !n.data.draft && n.data.type === 'freeform'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---
<PageLayout title="Freeform — Garden" description="Essays, reflections, and extended explorations of ideas.">
  <main style="max-width:48rem; margin:0 auto; padding:4rem 3rem;">
    <nav class="text-sm mb-6">
      <a href="/" class="text-gruv-fg4 hover:text-gruv-blue-b transition-colors">Home</a>
      <span class="text-gruv-bg3 mx-2">/</span>
      <a href="/garden" class="text-gruv-fg4 hover:text-gruv-blue-b transition-colors">Garden</a>
      <span class="text-gruv-bg3 mx-2">/</span>
      <span class="text-gruv-yellow-b">Freeform</span>
    </nav>

    <h1 class="text-4xl font-bold text-gruv-fg mb-2">Freeform</h1>
    <p class="text-gruv-fg4 text-lg mb-10">
      Essays and extended explorations. {notes.length} entries.
    </p>

    <ul class="flex flex-col gap-5">
      {notes.map((note) => <NoteCard note={note} />)}
    </ul>

    {notes.length === 0 && (
      <p class="text-gruv-fg4">No freeform notes yet — check back soon.</p>
    )}
  </main>
</PageLayout>
